# Netlify configuration for Solo Project 2 — Cloud Collection Manager
# Deploy the frontend (static site) from the public folder.

[build]
  publish = "public"
  # If your Netlify site is built from repo root, set base = "Solo Project 2" in the Netlify UI
  # and keep publish = "public"

# Optional: redirect SPA fallback (not required for this single-page app)
# [[redirects]]
#   from = "/*"
#   to = "/index.html"
#   status = 200
